apiVersion: v1
kind: Service
metadata:
  name: {{ include "monitor-api.fullname" . }}
  labels:
    {{- include "monitor-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monitor-api.selectorLabels" . | nindent 4 }}
