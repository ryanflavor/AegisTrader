apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "monitor-api.fullname" . }}
  labels:
    {{- include "monitor-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}