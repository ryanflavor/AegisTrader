apiVersion: v1
kind: Service
metadata:
  name: {{ include "monitor-ui.fullname" . }}
  labels:
    {{- include "monitor-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "monitor-ui.selectorLabels" . | nindent 4 }}
