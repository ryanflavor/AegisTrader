# AegisTrader Deployment Configuration
# This file contains deployment variables

# Docker image names (from docker-compose build)
DOCKER_API_IMAGE=aegistrader-monitor-api
DOCKER_UI_IMAGE=aegistrader-monitor-ui
DOCKER_TRADING_IMAGE=aegistrader-trading-service

# Kind cluster configuration
KIND_CLUSTER_NAME=aegis-local
KIND_CONTROL_PLANE=${KIND_CLUSTER_NAME}-control-plane

# Kubernetes configuration
K8S_NAMESPACE=aegis-trader
HELM_RELEASE_NAME=aegis-trader
HELM_DIR=helm

# Service names (as deployed in K8s)
API_SERVICE_NAME=${HELM_RELEASE_NAME}-monitor-api
UI_SERVICE_NAME=${HELM_RELEASE_NAME}-monitor-ui
NATS_SERVICE_NAME=${HELM_RELEASE_NAME}-nats

# Port configuration (using original ports)
API_PORT=8100
UI_PORT=3100
NATS_PORT=4222
NATS_FORWARD_PORT=4222

# Container port configuration
API_CONTAINER_PORT=8100
UI_CONTAINER_PORT=3100
NATS_CONTAINER_PORT=4222

# NATS JetStream configuration
NATS_JS_DOMAIN=aegis
NATS_KV_BUCKET=service-registry

# Service URLs
NATS_URL=nats://${NATS_SERVICE_NAME}:${NATS_PORT}
API_URL=http://${API_SERVICE_NAME}:${API_PORT}
UI_URL=http://${UI_SERVICE_NAME}:${UI_PORT}

# External URLs (for UI to access API)
NEXT_PUBLIC_API_URL=http://${API_SERVICE_NAME}:${API_PORT}

# Logging configuration
LOG_LEVEL=INFO

# Build configuration
VERSION_DATE_FORMAT=%Y%m%d-%H%M%S
