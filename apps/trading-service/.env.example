# Trading Service Environment Variables

# NATS connection URL
NATS_URL=nats://localhost:4222

# NATS KV Store bucket name for service registry
# Should match the bucket name configured in Helm deployment
NATS_KV_BUCKET=service-registry
