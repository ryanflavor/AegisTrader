# External dependencies only
# Local packages (aegis-sdk and shared-contracts) are installed separately in Dockerfile

# Dependencies needed by aegis-sdk
nats-py>=2.0.0
pydantic>=2.0.0
msgpack>=1.0.0
